import { user, userCreate, userUpdate, userRead, usersList, userLogin } from './user.schema';
import { course, courseCreate, courseUpdate, coursesList } from './course.schema';

export {
	user,
	userCreate,
	userUpdate,
	userRead,
	usersList,
	userLogin,
	course,
	courseCreate,
	courseUpdate,
	coursesList,
};
